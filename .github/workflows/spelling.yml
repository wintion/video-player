name: Spelling

on:
  push:
      branches: [main]
  pull_request:
      branches: [main]

jobs:
  typos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: crate-ci/typos@master
